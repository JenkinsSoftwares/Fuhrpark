<UserControl x:Class="MusicApp.Moduls.Selection.SelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MusicApp.Moduls.Selection"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- List of Bands -->
        <Label Grid.Row="0" Grid.Column="0" Background="Black"
                            Foreground="WhiteSmoke" Content="Bandnames" />
        <ListBox Margin="5" Grid.Row="1" Grid.Column="0" Background="WhiteSmoke"
                 ItemsSource="{Binding BandList}" SelectedItem="{Binding SelectedItem}"
                 DisplayMemberPath="Name" IsEnabled="{Binding EnableBandList}">
        </ListBox>
    </Grid>
</UserControl>